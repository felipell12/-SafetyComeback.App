@page
@model SafetyComeback.App.FrontEnd.Pages.FPrograma.ListProgramasModel
@{
    ViewData["Title"] = "Listado Programas";
}
<div class="container">
    @if (TempData["mensaje"] != null)
    {
        <div class="alert alert-warning alert-dimissible fade show" role="alert">
            @TempData["mensaje"]
            <button type="button" class="close" date-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    }
    <div class="row">
        <div class="col-sm-6">
            <h3>Lista de Programas</h3>
        </div>
        <div class="col-sm-6">
            <a class="btn btn-success d-block" asp-page="./CreatePrograma">Crear</a>
        </div>
    </div>
    <div class="row">
        @if (Model.listProgramas.Count()>0)
        {
            <table class="table table-bordered table-striped">
                
                    <thead>
                        <tr>
                            <td>Id</td>
                            <td>Nombre</td>
                            <td>Facultad Id</td>
                            <td>Acciones</td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var programa in Model.listProgramas)
                        {
                        <tr>
                            <td>@programa.Id</td>
                            <td>@programa.Nombre</td>
                            <td>@programa.Facultad.Nombre</td>
                            <td>
                                <a class="btn btn-warning" asp-page="./EditPrograma" 
                            asp-route-programaId="@programa.Id">
                                    <i class="fas fa-address-book fa-lg" aria-hidden="true"></i>
                            </a>
                                <a class="btn btn-danger" asp-page="./DeletePrograma" 
                            asp-route-programaId="@programa.Id">
                                    <i class="fas fa-address-book fa-lg" aria-hidden="true"></i>
                            </a>
                                <a class="btn btn-primary table-btn" asp-page="./DetailsPrograma" 
                            asp-route-programaId="@programa.Id">
                                    <i class="fas fa-info-circle fa-lg" aria-hidden="true"></i>
                            </a>
                            </td>
                        </tr>
                        }
                    </tbody>
                   
            </table>
        }
        else
        {
            <p>No existen registros</p>
        }
    </div>

</div>